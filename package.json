{
  "name": "tencent-vod-aigc-web",
  "private": true,
  "scripts": {
    "build:web": "esbuild frontend/previs_editor.js --bundle --minify --format=esm --platform=browser --target=es2022 --external:../../scripts/app.js --outfile=web/previs_editor.js && node scripts/clean-web-bundle.mjs"
  },
  "devDependencies": {
    "@sparkjsdev/spark": "^2.1.0",
    "esbuild": "^0.25.9",
    "three": "^0.180.0"
  }
}
